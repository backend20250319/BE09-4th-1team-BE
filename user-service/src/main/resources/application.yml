server:
  port: 0
  forward-headers-strategy: framework

spring:
  application:
    name: PLAYVOICE-USER-SERVICE
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 2MB