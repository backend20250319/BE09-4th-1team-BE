server:
  port: 8001
  forward-headers-strategy: framework

spring:
  application:
    name: PLAYVOICE-USER-SERVICE
  profiles:
    active: dev
  data:
    redis:
      host: redis
      port: 6379
      timeout: 6